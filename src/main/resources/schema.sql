CREATE TABLE CAD_COR (
	ID INT, 
	DESCRICAO VARCHAR(100),
	PRIMARY KEY (ID));

CREATE TABLE CAD_VEICULO (
	PLACA VARCHAR(8), 
	ANO_MODELO INT,
	ATUALIZADO_EM DATE,
	ID_COR INT,
	ANO_FABRICACAO INT,
	ATIVO BOOLEAN,
	PRIMARY KEY (PLACA),
	FOREIGN KEY (ID_COR) REFERENCES CAD_COR(ID));
	
